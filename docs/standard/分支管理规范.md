# 代码分支管理规范

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.0.0 |
| 创建日期 | 2024-12-28 |
| 适用范围 | AI PPTist System 项目 |
| 更新历史 | 初始版本 |

---

## 1. 项目概述

### 1.1 项目结构

AI PPTist System 是一个前后端分离的智能PPT生成系统，采用独特的子模块管理方式：

```
ai-pptist-system/                 # 主目录（后端项目）
├── backend/                      # 后端代码（自主开发）
├── frontend/                     # 前端代码（子模块，fork自开源项目）
├── docs/                         # 项目文档
├── docker/                       # Docker配置
└── ...
```

### 1.2 项目特点

1. **前端**：fork自 [npmfect/pptist](https://github.com/npmfect/pptist) 开源项目
2. **后端**：完全自主开发的Python FastAPI项目
3. **集成方式**：通过Git子模块将前端代码引入主项目
4. **开发模式**：前后端独立开发，通过API进行通信

### 1.3 特殊挑战

1. **上游同步**：需要定期同步前端子模块与上游官方库
2. **版本关联**：前后端代码需要保持版本兼容性
3. **团队协作**：多人协作时需要清晰的分支管理策略

---

## 2. 分支管理模型

本项目采用 **Enhanced Git Flow** 模型，结合 Git Flow 和 GitHub Flow 的优点，并针对子模块管理进行优化。

### 2.1 分支类型概览

```
┌─────────────────────────────────────────────────────────────────┐
│                        主目录分支结构                             │
├─────────────────────────────────────────────────────────────────┤
│  master (生产)                                                   │
│    ├── develop (开发主分支)                                       │
│    │     ├── feature/* (功能分支)                                │
│    │     ├── fix/* (修复分支)                                    │
│    │     └── release/* (发布分支)                                │
│    └── hotfix/* (紧急修复分支)                                   │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│                      子模块 frontend 分支结构                     │
├─────────────────────────────────────────────────────────────────┤
│  master (上游同步)                                               │
│    ├── integration/ai-pptist (集成稳定分支)                       │
│    │     ├── develop (开发主分支)                                │
│    │     │     ├── feature/* (功能分支)                          │
│    │     │     └── sync/* (同步临时分支)                         │
│    │     └── release/* (发布分支)                                │
│    └── hotfix/* (紧急修复分支)                                   │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 主目录分支定义

| 分支 | 命名规范 | 来源 | 目标 | 保护规则 |
|------|----------|------|------|----------|
| **master** | - | develop/merge | 生产部署 | 🔒 只读，需要PR |
| **develop** | - | master/init | master/merge | 🔒 需要PR |
| **feature** | feature/xxx | develop | develop | ✅ 可直接推送 |
| **fix** | fix/xxx | develop | develop | ✅ 可直接推送 |
| **release** | release/vX.Y.Z | develop | master + develop | 🔒 需要PR |
| **hotfix** | hotfix/xxx | master | master + develop | 🔒 需要PR |

### 2.3 子模块 frontend 分支定义

| 分支 | 命名规范 | 来源 | 目标 | 保护规则 |
|------|----------|------|------|----------|
| **master** | - | upstream/sync | - | 🔒 只读，仅同步 |
| **integration/ai-pptist** | - | develop/merge | 主目录/submodule | 🔒 需要PR |
| **develop** | - | integration/init | integration/merge | 🔒 需要PR |
| **feature** | feature/xxx | develop | develop | ✅ 可直接推送 |
| **sync** | sync/upstream-YYYYMMDD | master | develop/merge | ✅ 可直接推送 |
| **release** | release/vX.Y.Z | develop | integration | 🔒 需要PR |
| **hotfix** | hotfix/xxx | integration | integration + develop | 🔒 需要PR |

---

## 3. 分支职责详解

### 3.1 主目录分支

#### 3.1.1 master 分支

**职责**：生产环境分支，始终保持稳定可部署状态

**规则**：
- ✅ 接受来自 develop 的合并（功能完成）
- ✅ 接受来自 hotfix 的合并（紧急修复）
- ❌ 禁止直接提交
- ❌ 禁止功能分支直接合并
- 🏷️ 每次合并打 tag：`vX.Y.Z`

**示例**：
```bash
# 合并 release 分支
git checkout master
git merge --no-ff release/v1.2.0
git tag -a v1.2.0 -m "Release version 1.2.0"
git push origin master --tags
```

#### 3.1.2 develop 分支

**职责**：开发主分支，集成所有功能开发

**规则**：
- ✅ 接受来自 feature 的合并
- ✅ 接受来自 fix 的合并
- ✅ 接受来自 release 的合并（发布后）
- ❌ 禁止直接提交到 master
- 📦 保持与子模块 integration/ai-pptist 同步

**示例**：
```bash
# 合并功能分支
git checkout develop
git merge --no-ff feature/banana-generation
```

#### 3.1.3 feature/* 分支

**职责**：新功能开发分支

**命名规范**：
```
feature/<功能模块>-<功能描述>
feature/banana-generation        # Banana图片生成功能
feature/ocr-enhancement          # OCR增强功能
feature/user-authentication      # 用户认证功能
```

**生命周期**：
1. 从 develop 拉出
2. 开发完成后合并回 develop
3. 合并后可删除

**示例**：
```bash
# 创建功能分支
git checkout develop
git checkout -b feature/banana-generation

# 开发完成后合并
git checkout develop
git merge --no-ff feature/banana-generation
git branch -d feature/banana-generation
```

#### 3.1.4 fix/* 分支

**职责**：非紧急bug修复分支

**命名规范**：
```
fix/<模块>-<问题描述>
fix/ocr-coordinate-error         # OCR坐标错误修复
fix/image-upload-failure         # 图片上传失败修复
```

#### 3.1.5 release/* 分支

**职责**：发布准备分支，进行最后的测试和修复

**命名规范**：
```
release/vX.Y.Z
release/v1.2.0
release/v2.0.0
```

**生命周期**：
1. 从 develop 拉出
2. 只进行bug修复，不添加新功能
3. 测试通过后合并到 master 和 develop
4. 合并后删除

#### 3.1.6 hotfix/* 分支

**职责**：生产环境紧急修复分支

**命名规范**：
```
hotfix/<问题描述>
hotfix/critical-security-fix     # 紧急安全修复
hotfix-production-crash          # 生产环境崩溃修复
```

**生命周期**：
1. 从 master 拉出
2. 修复后合并到 master 和 develop
3. 合并后删除

### 3.2 子模块 frontend 分支

#### 3.2.1 master 分支

**职责**：与上游官方库保持同步

**规则**：
- 🔄 定期从 upstream 同步
- 🔒 只读分支，不进行开发
- 📅 同步频率：每周一次或重大更新时
- 📝 同步时创建 sync 分支

**同步流程**：
```bash
# 1. 添加上游仓库（如果还没有）
git remote add upstream https://github.com/npmfect/pptist.git

# 2. 获取上游更新
git fetch upstream

# 3. 创建同步分支
git checkout master
git checkout -b sync/upstream-$(date +%Y%m%d)

# 4. 合并上游代码
git merge upstream/master

# 5. 解决冲突（如果有）
# ... 解决冲突 ...

# 6. 推送到远端
git push origin sync/upstream-$(date +%Y%m%d)

# 7. 创建PR到 master
# 在GitHub上创建 PR: sync/upstream-20241228 -> master

# 8. PR审核通过后，合并到 master
# 9. 更新 master
git checkout master
git pull origin master
```

#### 3.2.2 integration/ai-pptist 分支

**职责**：与主目录集成的稳定分支

**规则**：
- 🎯 作为主目录的子模块引用
- ✅ 接受来自 develop 的合并（功能稳定后）
- ✅ 接受来自 hotfix 的合并（紧急修复）
- 🔒 需要PR才能合并
- 🔄 定期同步到主目录子模块

**更新主目录子模块引用**：
```bash
# 在子模块中
cd frontend
git checkout integration/ai-pptist
git pull origin integration/ai-pptist

# 在主目录中
cd ..
git add frontend
git commit -m "chore: 更新frontend子模块到integration/ai-pptist最新提交"
git push origin develop  # 假设在develop分支
```

#### 3.2.3 develop 分支

**职责**：功能开发主分支

**规则**：
- 🚀 接受来自 feature 的合并
- 🚀 接受来自 sync 的合并（上游更新）
- 📦 开发测试环境使用此分支
- 🔄 定期合并到 integration/ai-pptist

#### 3.2.4 feature/* 分支

**职责**：功能开发分支

**命名规范**：
```
feature/<功能模块>-<功能描述>
feature/banana-integration       # Banana功能集成
feature/ocr-coordinate-fix       # OCR坐标修复
```

**生命周期**：
1. 从 develop 拉出
2. 开发完成后合并回 develop
3. 功能稳定后，develop 合并到 integration

#### 3.2.5 sync/* 分支

**职责**：上游同步临时分支

**命名规范**：
```
sync/upstream-YYYYMMDD
sync/upstream-20241228
```

**用途**：
- 从 upstream/master 同步代码
- 解决同步冲突
- 测试上游更新是否兼容
- 创建PR到 master 和 develop

---

## 4. 工作流程

### 4.1 新功能开发流程

```
┌─────────────────────────────────────────────────────────────┐
│                     功能开发完整流程                          │
└─────────────────────────────────────────────────────────────┘

主目录：
  1. feature/banana-gen (develop)
     ↓ 开发
  2. 合并到 develop
     ↓ 测试
  3. release/v1.2.0 (develop)
     ↓ 发布准备
  4. 合并到 master + 标签

子模块：
  1. feature/banana-integration (develop)
     ↓ 开发
  2. 合并到 develop
     ↓ 测试
  3. 合并到 integration/ai-pptist
     ↓ 更新子模块引用
  4. 主目录更新 frontend 子模块
```

### 4.2 上游同步流程

```
┌─────────────────────────────────────────────────────────────┐
│                   上游同步完整流程                             │
└─────────────────────────────────────────────────────────────┘

1. 创建同步分支
   master → sync/upstream-20241228

2. 合并上游代码
   sync + upstream/master

3. 解决冲突
   （如有冲突手动解决）

4. 推送并创建PR
   sync/upstream-20241228 → master

5. 合并到 master
   PR审核通过后合并

6. 创建更新develop的PR
   master → develop

7. 测试兼容性
   在develop分支测试

8. 合并到develop
   确认无问题后合并
```

### 4.3 紧急修复流程

```
┌─────────────────────────────────────────────────────────────┐
│                   紧急修复完整流程                             │
└─────────────────────────────────────────────────────────────┘

主目录：
  1. hotfix/security-fix (master)
     ↓ 修复
  2. 合并到 master + develop
     ↓ 部署
  3. 打标签 v1.2.1

子模块：
  1. hotfix/ui-crash (integration/ai-pptist)
     ↓ 修复
  2. 合并到 integration + develop
     ↓ 更新子模块引用
  3. 主目录更新 frontend
```

---

## 5. 版本管理规范

### 5.1 版本号规范

采用语义化版本控制 [Semantic Versioning 2.0.0](https://semver.org/)

```
MAJOR.MINOR.PATCH

例：1.2.0
  MAJOR (1):   重大版本更新，不兼容的API变更
  MINOR (2):   功能更新，向后兼容
  PATCH (0):   Bug修复，向后兼容
```

### 5.2 标签规范

**主目录标签**：
```bash
v1.0.0    # 首个稳定版本
v1.1.0    # 新增功能
v1.1.1    # Bug修复
v2.0.0    # 重大版本更新
```

**子模块标签**：
```bash
frontend-v1.0.0    # 前端版本1.0.0
integration-v1.2.0  # 集成版本1.2.0
```

### 5.3 版本发布检查清单

- [ ] 所有功能测试通过
- [ ] 代码审查完成
- [ ] 文档更新完成
- [ ] CHANGELOG.md 更新
- [ ] 版本号更新
- [ ] 创建版本标签
- [ ] 部署到测试环境
- [ ] 测试环境验证
- [ ] 部署到生产环境

---

## 6. 提交规范

### 6.1 提交消息规范

采用 [Conventional Commits](https://www.conventionalcommits.org/) 规范：

```
<type>(<scope>): <subject>

<body>

<footer>
```

**类型 (type)**：
- `feat`: 新功能
- `fix`: Bug修复
- `docs`: 文档更新
- `style`: 代码格式（不影响功能）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具链相关

**示例**：
```bash
feat(banana): 添加图片生成Mock模式支持

实现Mock模式用于开发测试，减少AI API调用。

主要变更:
- 添加 enable_banana_image_mock 配置项
- 实现 _generate_mock_slide 方法
- 创建 banana_image_mock.json 测试数据

Closes #123
```

### 6.2 提交频率

- **小步快跑**：每个功能点完成后立即提交
- **原子提交**：每次提交只做一件事
- **清晰描述**：提交消息清晰描述改动内容

---

## 7. 代码审查规范

### 7.1 Pull Request 规范

**PR 标题**：
```
<type>: <简短描述>

feat(banana): 添加图片生成Mock模式支持
fix(ocr): 修复坐标转换使用真实图片尺寸
docs: 更新分支管理规范文档
```

**PR 描述模板**：
```markdown
## 变更类型
- [ ] 新功能
- [ ] Bug修复
- [ ] 重构
- [ ] 文档更新
- [ ] 性能优化

## 变更说明
<!-- 详细描述本次变更的内容 -->

## 测试情况
- [ ] 单元测试通过
- [ ] 集成测试通过
- [ ] 手动测试通过

## 相关Issue
Closes #123

## 截图/日志
<!-- 如果有UI变更，请提供截图 -->

## 检查清单
- [ ] 代码符合规范
- [ ] 添加了必要的注释
- [ ] 更新了相关文档
- [ ] 没有引入新的警告
```

### 7.2 审查要点

**代码质量**：
- ✅ 代码逻辑清晰
- ✅ 命名规范合理
- ✅ 错误处理完善
- ✅ 没有硬编码配置

**测试覆盖**：
- ✅ 单元测试充分
- ✅ 边界条件测试
- ✅ 异常情况测试

**文档完整**：
- ✅ API文档更新
- ✅ 注释清晰
- ✅ 变更日志更新

---

## 8. 分支保护规则

### 8.1 主目录保护规则

| 分支 | 保护项 | 规则 |
|------|--------|------|
| master | 🔒 推送保护 | 禁止直接推送，需要PR |
| master | ✅ 审查要求 | 至少1人审查批准 |
| master | 🧪 测试要求 | CI测试通过 |
| master | 📊 状态检查 | 所有检查通过 |
| develop | ✅ 审查要求 | 至少1人审查批准 |
| develop | 🧪 测试要求 | CI测试通过 |

### 8.2 子模块保护规则

| 分支 | 保护项 | 规则 |
|------|--------|------|
| master | 🔒 推送保护 | 禁止直接推送，需要PR |
| master | 🔄 同步检查 | 只能从sync分支合并 |
| integration/ai-pptist | ✅ 审查要求 | 至少1人审查批准 |
| integration/ai-pptist | 🧪 测试要求 | CI测试通过 |
| develop | ✅ 审查要求 | 至少1人审查批准 |

---

## 9. 子模块管理规范

### 9.1 子模块更新流程

**场景1：主目录需要使用子模块新功能**
```bash
# 1. 在子模块中完成开发并合并到 integration/ai-pptist
cd frontend
git checkout integration/ai-pptist
git pull origin integration/ai-pptist

# 2. 在主目录中更新子模块引用
cd ..
git add frontend
git commit -m "chore: 更新frontend子模块到最新版本"

# 3. 推送主目录
git push origin develop
```

**场景2：克隆项目时初始化子模块**
```bash
git clone https://github.com/domonic18/ai-pptist-system.git
cd ai-pptist-system
git submodule init
git submodule update
# 或者一步到位
git clone --recursive https://github.com/domonic18/ai-pptist-system.git
```

### 9.2 子模块版本锁定

主目录的 `.gitmodules` 文件：
```ini
[submodule "frontend"]
    path = frontend
    url = https://github.com/domonic18/ai-pptist.git
    branch = integration/ai-pptist
```

**注意**：主目录通过commit记录锁定子模块的具体提交，确保团队所有人使用相同版本的子模块代码。

### 9.3 子模块冲突处理

当主目录和子模块同时修改导致冲突时：

```bash
# 1. 更新子模块到正确版本
cd frontend
git checkout integration/ai-pptist
git pull origin integration/ai-pptist
cd ..

# 2. 在主目录中提交子模块更新
git add frontend
git commit -m "chore: 解决子模块冲突"

# 3. 继续主目录的其他合并
```

---

## 10. 团队协作规范

### 10.1 角色与权限

| 角色 | 主目录权限 | 子模块权限 | 职责 |
|------|------------|------------|------|
| **Maintainer** | 所有分支 | 所有分支 | 代码审查、发布管理 |
| **Developer** | feature/* | feature/* | 功能开发、Bug修复 |
| **Contributor** | PR提交 | PR提交 | 提交PR、参与讨论 |

### 10.2 工作分配

**功能开发**：
1. 在 Issue 中创建任务
2. 分配给开发者
3. 创建对应的 feature 分支
4. 开发完成后提交 PR
5. 代码审查通过后合并

**Bug修复**：
1. 在 Issue 中报告问题
2. Maintainer 评估优先级
3. 分配给开发者
4. 创建 fix/hotfix 分支
5. 修复完成后提交 PR

### 10.3 沟通规范

**日常沟通**：
- 使用 GitHub Issues 跟踪任务和问题
- 使用 Pull Requests 进行代码审查
- 使用 Discussions 进行技术讨论

**发布沟通**：
- 提前1周发布版本计划
- 使用 Release Notes 记录变更
- 重大变更提前通知

---

## 11. 最佳实践

### 11.1 分支管理最佳实践

✅ **推荐做法**：
- 保持分支短生命周期（< 2周）
- 定期同步主分支到功能分支
- 使用描述性的分支名称
- 合并后删除已完成的分支
- 使用 `--no-ff` 合并保留历史

❌ **避免做法**：
- 在公共分支上直接开发
- 长期存在的功能分支
- 不清楚的分支命名
- 遗留的僵尸分支
- 直接推送到保护分支

### 11.2 子模块管理最佳实践

✅ **推荐做法**：
- 定期同步上游更新
- 及时更新主目录子模块引用
- 明确子模块分支用途
- 使用固定版本而不是 latest

❌ **避免做法**：
- 在子模块 master 上开发
- 子模块版本混乱
- 忘记更新子模块引用
- 频繁切换子模块分支

### 11.3 团队协作最佳实践

✅ **推荐做法**：
- 小步快跑，频繁提交
- 及时进行代码审查
- 保持良好的提交历史
- 编写清晰的文档
- 及时解决冲突

❌ **避免做法**：
- 大批量代码一次性提交
- 长时间不合并代码
- 不清晰的提交消息
- 缺少文档和注释
- 忽略代码冲突

---

## 12. 工具推荐

### 12.1 Git 工具

- **命令行**：git, git-submodule
- **GUI工具**：SourceTree, GitKraken, GitHub Desktop
- **VSCode插件**：GitLens, Git Graph

### 12.2 CI/CD 工具

- **GitHub Actions**：自动化测试、部署
- **GitLab CI**：完整的DevOps流程
- **Jenkins**：企业级CI/CD

### 12.3 项目管理工具

- **GitHub Projects**：看板、里程碑
- **Jira**：敏捷开发管理
- **Notion**：团队协作文档

---

## 13. 附录

### 13.1 常用命令速查

```bash
# ===== 主目录操作 =====
# 创建功能分支
git checkout develop
git checkout -b feature/your-feature

# 合并功能分支
git checkout develop
git merge --no-ff feature/your-feature

# 创建发布分支
git checkout develop
git checkout -b release/v1.2.0

# 紧急修复
git checkout master
git checkout -b hotfix/critical-fix

# ===== 子模块操作 =====
# 更新子模块
git submodule update --remote frontend

# 查看子模块状态
git submodule status

# 同步上游
git remote add upstream https://github.com/npmfect/pptist.git
git fetch upstream
git checkout -b sync/upstream-$(date +%Y%m%d)
git merge upstream/master

# 创建标签
git tag -a v1.2.0 -m "Release version 1.2.0"
git push origin v1.2.0
```

### 13.2 紧急情况处理

**子模块上游重大更新不兼容**：
1. 暂停同步
2. 评估影响范围
3. 在 develop 分支进行适配
4. 测试通过后再合并

**主目录和子模块版本不匹配**：
1. 确认主目录需要的子模块版本
2. 在主目录中锁定子模块版本
3. 更新文档说明版本要求

**团队成员代码冲突**：
1. 及早沟通，避免重复开发
2. 使用不同的功能分支
3. 定期合并到 develop
4. 及时解决冲突

---

## 14. 变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|----------|------|
| v1.0.0 | 2024-12-28 | 初始版本 | Claude Code |

---

## 15. 参考文档

- [Git Flow](https://nvie.com/posts/a-successful-git-branching-model/)
- [GitHub Flow](https://docs.github.com/en/get-started/quickstart/github-flow)
- [Semantic Versioning](https://semver.org/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [Git Submodule Documentation](https://git-scm.com/docs/git-submodule)
